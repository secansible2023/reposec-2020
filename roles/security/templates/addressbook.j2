security {
    {% for source_address in source_addresses.split('\n') %}
    address-book {
        INTERNET_NTL {
            address {{ source_address }};
            address-set blacklist_ansible_ntl {
                address {{ source_address }};
            }
        }
    }
    {% endfor %}
}