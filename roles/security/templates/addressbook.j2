{% if inventory_hostname == 'SRX_TEST' %}
security {
    zones {
        security-zone VR0_untrust {
            {% for source_address in source_addresses.split('\n') %}
            address-book {
                address {{ source_address }} {{ source_address }};
                address-set blacklist-vnportal {
                    address {{ source_address }};       
                }
            }
            {% endfor %}
        }
    }
}
{% endif %}
